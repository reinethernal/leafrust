# Place trained TFLite weights here as:
#   plantvillage_mobilenet.tflite
#
# Train/export with:
#   python ../scripts/train_mobilenet.py --data /path/to/PlantVillage --out ./plantvillage_mobilenet.tflite
#
# Until the file exists, LeafRust runs in on-device demo mode
# (HSV severity is real; class labels use a lightweight local heuristic).
